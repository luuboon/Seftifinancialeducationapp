
  # SEFTI Financial Education App

  This is a code bundle for SEFTI Financial Education App. 
  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
